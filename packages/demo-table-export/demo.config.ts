/**
 * 演示包自描述信息，由主包的 `demos.ts` 聚合。
 *
 * `name` 同时是路由前缀，用于主包把路由归组到该演示的菜单项下。
 */
export default {
  name: 'tableExport',
  label: 'Table 导出',
  routesFolder: 'packages/demo-table-export/src/pages',
}
